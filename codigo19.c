#include <stdio.h>
#include <stdlib.h>

int main(){
	int numero, i;
	
	printf("Digite a tabuada desejada: ");
	scanf("%i", &numero);
	
	for(j = 1; j <= 10; j++){
		printf("%i x %i = %i\n", numero, i, numero * i);
	}
}
